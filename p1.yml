- hosts: webservers
  tasks:
   - file:
        src: /tmp/test
        state: directory
   - copy:
        src: /etc/fstab
        dest: /tmp/test
   - service:
        name: crond
        state: restarted